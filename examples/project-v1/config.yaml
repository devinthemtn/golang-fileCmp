# Project Configuration v1
name: "example-project"
version: "1.0.0"

database:
  host: "localhost"
  port: 5432
  name: "myapp_db"
  user: "admin"
  password: "secret123"

server:
  host: "0.0.0.0"
  port: 8080
  timeout: 30

features:
  auth_enabled: true
  logging_level: "info"
  cache_enabled: false
  max_connections: 100

api:
  rate_limit: 1000
  allowed_origins:
    - "https://example.com"
    - "https://www.example.com"

monitoring:
  enabled: false
  endpoint: "http://localhost:9090"
